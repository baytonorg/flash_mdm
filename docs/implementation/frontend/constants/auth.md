# `src/constants/auth.ts`

> Password length constraint constants used for validation across auth forms.

## Exports

| Name | Type | Description |
|------|------|-------------|
| `MIN_PASSWORD_LENGTH` | `number` | Minimum password length: `12` |
| `MAX_PASSWORD_LENGTH` | `number` | Maximum password length: `128` |
